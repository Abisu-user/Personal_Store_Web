"use client";

import { useEffect, useMemo, useState } from "react";
import { Accent, Appearance, Background, BackgroundRotation, Density, Theme, activeBackground, appearanceDefaults, applyAppearance, normalizeHexColor, readAppearance, saveAppearance } from "@/lib/appearance/preferences";

const options = {
  theme: [["system", "跟隨系統"], ["light", "淺色"], ["dark", "深色"]] as const,
  accent: [["blue", "靛藍"], ["violet", "紫羅蘭"], ["emerald", "翡翠綠"], ["rose", "玫瑰紅"]] as const,
  background: [["mist", "柔霧"], ["aurora", "極光"], ["paper", "紙感"], ["midnight", "午夜"]] as const,
  density: [["comfortable", "舒適"], ["compact", "緊湊"]] as const,
};
const focusPositions = [["left", "偏左", 18, 50], ["center", "置中", 50, 50], ["right", "偏右", 82, 50], ["top", "偏上", 50, 18], ["bottom", "偏下", 50, 82]] as const;
function hexToRgb(hex: string) { return [Number.parseInt(hex.slice(1, 3), 16), Number.parseInt(hex.slice(3, 5), 16), Number.parseInt(hex.slice(5, 7), 16)] as const; }
function rgbToHex(rgb: number[]) { return `#${rgb.map((value) => Math.max(0, Math.min(255, value)).toString(16).padStart(2, "0")).join("")}`.toUpperCase(); }

async function prepareImage(file: File) {
  const source = URL.createObjectURL(file);
  try {
    const image = new Image(); image.src = source; await new Promise<void>((resolve, reject) => { image.onload = () => resolve(); image.onerror = () => reject(new Error("image")); });
    const scale = Math.min(1, 1280 / image.naturalWidth, 720 / image.naturalHeight);
    const canvas = document.createElement("canvas"); canvas.width = Math.max(1, Math.round(image.naturalWidth * scale)); canvas.height = Math.max(1, Math.round(image.naturalHeight * scale));
    canvas.getContext("2d")?.drawImage(image, 0, 0, canvas.width, canvas.height);
    return { dataUrl: canvas.toDataURL("image/jpeg", 0.76), lowQuality: image.naturalWidth < 1600 || image.naturalHeight < 900 };
  } finally { URL.revokeObjectURL(source); }
}

export function AppearanceSettings() {
  const [appearance, setAppearance] = useState<Appearance>(appearanceDefaults); const [ready, setReady] = useState(false); const [imageNotice, setImageNotice] = useState<string | null>(null); const rgb = useMemo(() => hexToRgb(appearance.customColor), [appearance.customColor]);
  useEffect(() => { const stored = readAppearance(); applyAppearance(stored); const timer = window.setTimeout(() => { setAppearance(stored); setReady(true); }, 0); return () => window.clearTimeout(timer); }, []);
  function commit(next: Appearance) { setAppearance(next); saveAppearance(next); }
  function update<Key extends keyof Appearance>(key: Key, value: Appearance[Key]) { commit({ ...appearance, [key]: value } as Appearance); }
  function updateCustomColor(value: string) { commit({ ...appearance, accent: "custom" as Accent, customColor: normalizeHexColor(value, appearance.customColor) }); }
  function updateRgb(index: number, value: string) { if (value === "") return; const parsed = Number.parseInt(value, 10); if (!Number.isFinite(parsed)) return; const nextRgb = [...rgb]; nextRgb[index] = Math.max(0, Math.min(255, parsed)); updateCustomColor(rgbToHex(nextRgb)); }
  function reset() { setAppearance(appearanceDefaults); applyAppearance(appearanceDefaults); window.localStorage.removeItem("personal-vault:appearance:v1"); window.localStorage.removeItem("personal-vault:appearance:v2"); window.localStorage.removeItem("personal-vault:appearance:v3"); }
  async function chooseBackgrounds(files: FileList | null) {
    if (!files?.length) return; const selected = [...files];
    if (selected.some((file) => !["image/jpeg", "image/png", "image/webp"].includes(file.type) || file.size > 6_000_000)) { setImageNotice("請選擇單張 6 MB 以下的 JPG、PNG 或 WebP 圖片。"); return; }
    try {
      const images = await Promise.all(selected.slice(0, 4).map(prepareImage)); const merged = [...appearance.backgroundImages, ...images.map((item) => item.dataUrl)].slice(-4);
      commit({ ...appearance, backgroundImages: merged, backgroundActiveIndex: Math.max(0, merged.length - images.length), backgroundImage: merged[Math.max(0, merged.length - images.length)] });
      setImageNotice(images.some((item) => item.lowQuality) ? "已加入背景清單；其中有圖片低於建議 1600 × 900，放大裁切時可能失真。" : `已加入 ${images.length} 張背景圖片。`);
    } catch { setImageNotice("讀取圖片失敗，請改用 JPG、PNG 或 WebP。 "); }
  }
  function selectImage(index: number) { commit({ ...appearance, backgroundActiveIndex: index, backgroundImage: appearance.backgroundImages[index], backgroundRotation: "manual" }); }
  function removeImage(index: number) { const images = appearance.backgroundImages.filter((_, current) => current !== index); commit({ ...appearance, backgroundImages: images, backgroundActiveIndex: Math.min(appearance.backgroundActiveIndex, Math.max(0, images.length - 1)), backgroundImage: images[Math.min(appearance.backgroundActiveIndex, Math.max(0, images.length - 1))] }); setImageNotice(images.length ? "背景圖片已移除。" : "已移除所有自訂背景圖片。"); }
  const previewImage = activeBackground(appearance);

  return <section className="appearance-workspace" aria-busy={!ready}>
    <section className="appearance-preview"><div><p className="eyebrow">LIVE PREVIEW</p><h2>這是你的工作空間</h2><p>選擇會立即套用至所有頁面，並記住這台裝置的設計偏好。</p></div><div className="appearance-sample"><span>V</span><div><strong>今日焦點</strong><small>將常用資料整理得更舒服</small></div><i>◈</i></div></section>
    <section className="appearance-section"><div><p className="eyebrow">THEME</p><h2>明暗模式</h2></div><div className="appearance-options">{options.theme.map(([value, label]) => <button aria-pressed={appearance.theme === value} className={appearance.theme === value ? "appearance-choice active" : "appearance-choice"} key={value} onClick={() => update("theme", value as Theme)} type="button"><i className={`theme-swatch ${value}`} /><span>{label}</span></button>)}</div></section>
    <section className="appearance-section"><div><p className="eyebrow">ACCENT</p><h2>主色調</h2></div><div><div className="appearance-options accent-options">{options.accent.map(([value, label]) => <button aria-pressed={appearance.accent === value} className={appearance.accent === value ? "appearance-choice active" : "appearance-choice"} key={value} onClick={() => update("accent", value as Accent)} type="button"><i className={`accent-swatch ${value}`} /><span>{label}</span></button>)}<button aria-pressed={appearance.accent === "custom"} className={appearance.accent === "custom" ? "appearance-choice active custom-color-choice" : "appearance-choice custom-color-choice"} onClick={() => update("accent", "custom")} type="button"><i className="accent-swatch custom" style={{ background: appearance.customColor }} /><span>自訂色</span></button></div><div className="custom-color-controls"><label>調色盤<input aria-label="自訂主色調" onChange={(event) => updateCustomColor(event.target.value)} type="color" value={appearance.customColor} /></label><label>HEX<input aria-label="HEX 色碼" onChange={(event) => { if (/^#[0-9a-f]{6}$/i.test(event.target.value)) updateCustomColor(event.target.value); }} pattern="#[0-9A-Fa-f]{6}" value={appearance.customColor} /></label>{["R", "G", "B"].map((label, index) => <label key={label}>{label}<input aria-label={`${label} 色彩數值`} inputMode="numeric" max="255" min="0" onChange={(event) => updateRgb(index, event.target.value)} type="number" value={rgb[index]} /></label>)}</div></div></section>
    <section className="appearance-section"><div><p className="eyebrow">BACKGROUND</p><h2>工作區背景</h2></div><div><div className="appearance-options background-options">{options.background.map(([value, label]) => <button aria-pressed={appearance.background === value} className={appearance.background === value ? "appearance-choice active" : "appearance-choice"} key={value} onClick={() => update("background", value as Background)} type="button"><i className={`background-swatch ${value}`} /><span>{label}</span></button>)}</div><div className="background-image-controls"><label>上傳圖片（最多 4 張）<input accept="image/jpeg,image/png,image/webp" multiple onChange={(event) => void chooseBackgrounds(event.target.files)} type="file" /></label>{appearance.backgroundImages.length > 0 && <><div className="background-playlist" aria-label="背景圖片清單">{appearance.backgroundImages.map((image, index) => <div className={appearance.backgroundActiveIndex === index ? "background-thumbnail active" : "background-thumbnail"} key={`${image.slice(-24)}-${index}`}><button aria-label={`選擇第 ${index + 1} 張背景`} onClick={() => selectImage(index)} style={{ backgroundImage: `url("${image}")` }} type="button" /><button aria-label={`移除第 ${index + 1} 張背景`} className="background-image-remove" onClick={() => removeImage(index)} type="button">×</button></div>)}</div><label>切換方式<select aria-label="背景切換方式" onChange={(event) => update("backgroundRotation", event.target.value as BackgroundRotation)} value={appearance.backgroundRotation}><option value="manual">手動選擇</option><option value="login">每次登入／重新開啟時</option><option value="interval">依時間自動切換</option></select></label>{appearance.backgroundRotation === "interval" && <label>切換間隔<select aria-label="背景切換間隔" onChange={(event) => update("backgroundRotationMinutes", Number(event.target.value))} value={appearance.backgroundRotationMinutes}>{[5, 15, 30, 60].map((minutes) => <option key={minutes} value={minutes}>{minutes} 分鐘</option>)}</select></label>}<div className="background-preview-wrap"><strong>工作區預覽</strong><div aria-label="背景預覽" className="background-preview" style={{ backgroundImage: `linear-gradient(rgb(255 255 255 / 42%), rgb(255 255 255 / 42%)), url("${previewImage}")`, backgroundPosition: `center, ${appearance.backgroundPositionX}% ${appearance.backgroundPositionY}%`, backgroundSize: `cover, ${appearance.backgroundZoom}%` }}><span>預覽中的工作區</span></div></div><div className="crop-controls"><strong>自由裁切</strong><p>移動焦點或調整縮放，預覽與實際工作區會套用相同裁切。</p><label>水平焦點 <input aria-label="裁切水平焦點" max="100" min="0" onChange={(event) => update("backgroundPositionX", Number(event.target.value))} type="range" value={appearance.backgroundPositionX} /></label><label>垂直焦點 <input aria-label="裁切垂直焦點" max="100" min="0" onChange={(event) => update("backgroundPositionY", Number(event.target.value))} type="range" value={appearance.backgroundPositionY} /></label><label>放大 <input aria-label="裁切放大程度" max="180" min="100" onChange={(event) => update("backgroundZoom", Number(event.target.value))} type="range" value={appearance.backgroundZoom} /><output>{appearance.backgroundZoom}%</output></label>{focusPositions.map(([value, label, x, y]) => <button className={appearance.backgroundPosition === value ? "active" : ""} key={value} onClick={() => commit({ ...appearance, backgroundPosition: value, backgroundPositionX: x, backgroundPositionY: y })} type="button">{label}</button>)}</div><label>背景調色<input aria-label="背景調色" onChange={(event) => update("backgroundTint", event.target.value)} type="color" value={appearance.backgroundTint ?? "#FFFFFF"} /></label></>} {imageNotice && <p className="background-image-notice">{imageNotice}</p>}</div></div></section>
    <section className="appearance-section"><div><p className="eyebrow">LAYOUT</p><h2>資料密度</h2></div><div className="appearance-options">{options.density.map(([value, label]) => <button aria-pressed={appearance.density === value} className={appearance.density === value ? "appearance-choice active" : "appearance-choice"} key={value} onClick={() => update("density", value as Density)} type="button"><i className={`density-swatch ${value}`} /><span>{label}</span></button>)}</div></section>
    <button className="secondary-button appearance-reset" onClick={reset} type="button">還原預設外觀</button>
  </section>;
}
