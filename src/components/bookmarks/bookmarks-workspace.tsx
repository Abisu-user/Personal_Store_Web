"use client";

import { FocusEvent, FormEvent, useCallback, useMemo, useState } from "react";
import type { Bookmark, BookmarksWorkspaceData } from "@/lib/bookmarks/types";

type View = "all" | "favorite" | "pinned" | "archived" | "trash";
type DraftPreview = { hostname: string; title: string | null; description: string | null; imageUrl: string | null; faviconUrl: string | null };
type Confirmation = { id: string; title: string; action: "trash" | "permanent" };
const viewLabels: Record<View, string> = { all: "全部", favorite: "我的最愛", pinned: "置頂", archived: "封存", trash: "垃圾桶" };

function WebsitePreview({ bookmark }: { bookmark: Bookmark }) {
  const url = bookmark.detail?.url;
  let hostname = "網站連結"; let fallbackFaviconUrl: string | null = null;
  try { if (url) { const parsed = new URL(url); hostname = parsed.hostname.replace(/^www\./, ""); fallbackFaviconUrl = new URL("/favicon.ico", parsed.origin).toString(); } } catch { /* Existing saved records may be malformed. */ }
  const imageUrl = bookmark.detail?.favicon_url ?? fallbackFaviconUrl;
  return <a className={imageUrl ? "bookmark-preview has-image" : "bookmark-preview"} href={url} rel="noreferrer noopener" target="_blank"><span aria-hidden="true" className="bookmark-preview-fallback">{hostname.slice(0, 1).toUpperCase()}</span>{imageUrl && <img alt="" loading="lazy" referrerPolicy="no-referrer" src={imageUrl} />}<span>{hostname}</span></a>;
}

export function BookmarksWorkspace({ initialData, createMode = false }: { initialData: BookmarksWorkspaceData; createMode?: boolean }) {
  const [data, setData] = useState(initialData); const [error, setError] = useState<string | null>(null); const [pending, setPending] = useState(false);
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("all"); const [view, setView] = useState<View>("all"); const [confirmation, setConfirmation] = useState<Confirmation | null>(null);
  const [draftPreview, setDraftPreview] = useState<DraftPreview | null>(null); const [previewing, setPreviewing] = useState(false); const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set());

  const load = useCallback(async () => { const result = await fetch("/api/bookmarks", { cache: "no-store" }); if (!result.ok) { setError("目前無法讀取書籤。"); return; } setData(await result.json() as BookmarksWorkspaceData); }, []);
  const bookmarksForView = useCallback((bookmark: Bookmark) => { if (view === "trash") return Boolean(bookmark.deletedAt); if (bookmark.deletedAt) return false; if (view === "favorite") return bookmark.favorite && !bookmark.archived; if (view === "pinned") return bookmark.pinned && !bookmark.archived; if (view === "archived") return bookmark.archived; return !bookmark.archived; }, [view]);
  const filtered = useMemo(() => data.bookmarks.filter((bookmark) => { const text = `${bookmark.title} ${bookmark.description ?? ""} ${bookmark.detail?.url ?? ""} ${bookmark.tags.map((tag) => tag.name).join(" ")}`.toLowerCase(); return bookmarksForView(bookmark) && (category === "all" || bookmark.category?.id === category) && text.includes(query.toLowerCase()); }), [bookmarksForView, category, data.bookmarks, query]);
  const counts = useMemo(() => data.bookmarks.reduce<Record<View, number>>((total, bookmark) => { if (bookmark.deletedAt) { total.trash += 1; return total; } if (bookmark.archived) total.archived += 1; else { total.all += 1; if (bookmark.favorite) total.favorite += 1; if (bookmark.pinned) total.pinned += 1; } return total; }, { all: 0, favorite: 0, pinned: 0, archived: 0, trash: 0 }), [data.bookmarks]);
  const visibleSelectedIds = filtered.filter((bookmark) => selectedIds.has(bookmark.id)).map((bookmark) => bookmark.id);
  const allVisibleSelected = filtered.length > 0 && visibleSelectedIds.length === filtered.length;

  async function createBookmark(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const formElement = event.currentTarget; setPending(true); setError(null); const form = new FormData(formElement); const body = { url: String(form.get("url") ?? ""), title: String(form.get("title") ?? ""), description: String(form.get("description") ?? ""), categoryId: String(form.get("categoryId") ?? "") || null, tags: String(form.get("tags") ?? "").split(",").map((tag) => tag.trim()).filter(Boolean) }; const result = await fetch("/api/bookmarks", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); setPending(false); if (!result.ok) { const message = await result.json().catch(() => null); setError(message?.error ?? "無法儲存書籤。"); return; } formElement.reset(); setDraftPreview(null); }
  async function previewBookmark(event: FocusEvent<HTMLInputElement>) { const input = event.currentTarget; const url = input.value.trim(); if (!url) { setDraftPreview(null); return; } try { new URL(url); } catch { return; } setPreviewing(true); setError(null); try { const result = await fetch("/api/bookmarks/preview", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ url }) }); const payload = await result.json().catch(() => null) as DraftPreview | null; if (!result.ok || !payload || typeof payload.hostname !== "string") { setDraftPreview(null); return; } setDraftPreview(payload); const form = input.form; const title = form?.elements.namedItem("title"); const description = form?.elements.namedItem("description"); if (title instanceof HTMLInputElement && !title.value) title.value = payload.title ?? payload.hostname; if (description instanceof HTMLTextAreaElement && !description.value && payload.description) description.value = payload.description; } finally { setPreviewing(false); } }
  async function updateBookmark(id: string, action: "toggle_favorite" | "toggle_pinned" | "archive" | "unarchive" | "restore") { setPending(true); setError(null); const result = await fetch("/api/bookmarks", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id, action }) }); setPending(false); if (!result.ok) { setError("無法更新書籤狀態。"); return; } await load(); }
  async function executeSingle() { if (!confirmation) return; const endpoint = confirmation.action === "permanent" ? "DELETE" : "PATCH"; const body = confirmation.action === "permanent" ? { id: confirmation.id } : { id: confirmation.id, action: "trash" }; setPending(true); const result = await fetch("/api/bookmarks", { method: endpoint, headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); setPending(false); setConfirmation(null); if (!result.ok) { setError("無法更新書籤。"); return; } setSelectedIds((current) => { const next = new Set(current); next.delete(confirmation.id); return next; }); await load(); }
  async function bulkAction() { if (!visibleSelectedIds.length) return; const action = view === "trash" ? "permanent" : "trash"; if (!window.confirm(`確定${action === "permanent" ? "永久刪除" : "移至垃圾桶"}選取的 ${visibleSelectedIds.length} 筆書籤？`)) return; setPending(true); setError(null); const result = await fetch("/api/bookmarks", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ids: visibleSelectedIds, action }) }); setPending(false); if (!result.ok) { const message = await result.json().catch(() => null); setError(message?.error ?? "無法批量更新書籤。"); return; } setSelectedIds(new Set()); await load(); }
  function toggleAll() { setSelectedIds((current) => { const next = new Set(current); if (allVisibleSelected) filtered.forEach((bookmark) => next.delete(bookmark.id)); else filtered.forEach((bookmark) => next.add(bookmark.id)); return next; }); }
  function toggleOne(id: string) { setSelectedIds((current) => { const next = new Set(current); if (next.has(id)) next.delete(id); else next.add(id); return next; }); }

  const createContent = <><form className="bookmark-form" onSubmit={createBookmark}><h2>新增書籤</h2><input aria-label="網址" name="url" onBlur={(event) => void previewBookmark(event)} placeholder="貼上網址後自動帶入標題與預覽圖" required type="url" />{previewing && <p className="bookmark-preview-loading" role="status">正在取得連結預覽…</p>}{draftPreview && <div className="bookmark-draft-preview">{(draftPreview.imageUrl ?? draftPreview.faviconUrl) && <img alt="" referrerPolicy="no-referrer" src={draftPreview.imageUrl ?? draftPreview.faviconUrl ?? undefined} />}<div><strong>{draftPreview.title ?? draftPreview.hostname}</strong>{draftPreview.description && <span>{draftPreview.description}</span>}<small>{draftPreview.hostname}</small></div></div>}<input aria-label="標題" name="title" placeholder="標題會自動帶入，也可自行改寫" /><textarea aria-label="備註" name="description" placeholder="備註（選填）" rows={2} /><select aria-label="分類" defaultValue="" name="categoryId"><option value="">未分類</option>{data.categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><input aria-label="標籤" name="tags" placeholder="標籤，以逗號分隔" /><button className="button" disabled={pending} type="submit">{pending ? "儲存中…" : "儲存書籤"}</button></form></>;
  if (createMode) return <section className="bookmarks-workspace create-only">{error && <p className="notice error" role="alert">{error}</p>}{createContent}</section>;

  return <section className="bookmarks-workspace">{error && <p className="notice error" role="alert">{error}</p>}<div className="bookmark-view-tabs" role="tablist" aria-label="書籤狀態">{(Object.keys(viewLabels) as View[]).map((item) => <button aria-selected={view === item} className={view === item ? "active" : ""} key={item} onClick={() => setView(item)} role="tab" type="button">{viewLabels[item]} <span>{counts[item]}</span></button>)}</div><div className="bookmark-toolbar"><input aria-label="搜尋書籤" onChange={(event) => setQuery(event.target.value)} placeholder="搜尋標題、網址或標籤" value={query} /><select aria-label="篩選分類" onChange={(event) => setCategory(event.target.value)} value={category}><option value="all">所有分類</option>{data.categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div className="bulk-toolbar"><label><input aria-label="全選目前書籤" checked={allVisibleSelected} onChange={toggleAll} type="checkbox" /> 全選目前清單</label>{visibleSelectedIds.length > 0 && <button className="delete-button" disabled={pending} onClick={() => void bulkAction()} type="button">{view === "trash" ? `永久刪除 ${visibleSelectedIds.length} 筆` : `移至垃圾桶 ${visibleSelectedIds.length} 筆`}</button>}</div><div className="bookmark-list">{filtered.map((bookmark) => <article className="bookmark-card" key={bookmark.id}><label className="item-select"><input aria-label={`選取 ${bookmark.title}`} checked={selectedIds.has(bookmark.id)} onChange={() => toggleOne(bookmark.id)} type="checkbox" /></label><WebsitePreview bookmark={bookmark} /><div className="bookmark-card-content"><p className="bookmark-meta">{bookmark.category?.name ?? "未分類"}</p><a className="bookmark-title" href={bookmark.detail?.url} rel="noreferrer noopener" target="_blank">{bookmark.title}</a><p>{bookmark.description}</p><div className="bookmark-status">{bookmark.favorite && <span>★ 我的最愛</span>}{bookmark.pinned && <span>⌖ 已置頂</span>}{bookmark.archived && <span>已封存</span>}</div><div className="tag-line">{bookmark.tags.map((tag) => <span key={tag.id}>#{tag.name}</span>)}</div></div><div className="bookmark-actions">{bookmark.deletedAt ? <><button className="secondary-button" disabled={pending} onClick={() => void updateBookmark(bookmark.id, "restore")} type="button">還原</button><button className="delete-button" disabled={pending} onClick={() => setConfirmation({ id: bookmark.id, title: bookmark.title, action: "permanent" })} type="button">永久刪除</button></> : <><button className="secondary-button" disabled={pending} onClick={() => void updateBookmark(bookmark.id, "toggle_favorite")} type="button">{bookmark.favorite ? "取消收藏" : "收藏"}</button><button className="secondary-button" disabled={pending} onClick={() => void updateBookmark(bookmark.id, "toggle_pinned")} type="button">{bookmark.pinned ? "取消置頂" : "置頂"}</button><button className="secondary-button" disabled={pending} onClick={() => void updateBookmark(bookmark.id, bookmark.archived ? "unarchive" : "archive")} type="button">{bookmark.archived ? "取消封存" : "封存"}</button><button className="delete-button" disabled={pending} onClick={() => setConfirmation({ id: bookmark.id, title: bookmark.title, action: "trash" })} type="button">移至垃圾桶</button></>}</div></article>)}{filtered.length === 0 && <p className="lead">{view === "trash" ? "垃圾桶目前是空的。" : "尚無符合條件的書籤。"}</p>}</div>{confirmation && <div aria-modal="true" className="inline-dialog" role="alertdialog"><p>{confirmation.action === "permanent" ? `確定永久刪除「${confirmation.title}」？此操作無法復原。` : `確定將「${confirmation.title}」移至垃圾桶？你之後仍可還原。`}</p><div><button className="delete-button" disabled={pending} onClick={() => void executeSingle()} type="button">確認</button><button className="secondary-button" disabled={pending} onClick={() => setConfirmation(null)} type="button">取消</button></div></div>}</section>;
}
